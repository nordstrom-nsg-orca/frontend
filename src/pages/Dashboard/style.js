const style = theme => ({
  root: {
    width: '300px',
    height: '100px',
    margin: 'auto',
    marginTop: '50px',
    textAlign: 'center',
    padding: '30px',
    background: theme.background,
    color: theme.color,
  },
});

export default style;
